function fastPower(number, power) {
    return number ** power;
}

let result = fastPower(10,2);
console.log(result);

// berdasarkan big o notation
// speed constant
// time complexity : 0(1)
// space complexity : 0(1)
